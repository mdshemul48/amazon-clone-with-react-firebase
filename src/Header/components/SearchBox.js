import { Fragment } from 'react'

const SearchBox = () => {
    return (
        <Fragment>
            <input type="text" className="header__searchInput" />
        </Fragment>
    )
}

export default SearchBox;
